This module requires **xcaddy 2** to build, because it targets the Caddy 2 plugin API.

Example:

    xcaddy build \
        --with github.com/qwraith/caddy-auth-request \
        --with github.com/caddy-dns/cloudflare

